# Session Summary — 2026-03-30

## What Was Done

### 1. Folder restructuring
- Renamed `conjecture 1/` → `experiment/`
- Renamed `results/` → `gd_trajectories/` inside experiment
- Updated all hardcoded `"results"` references in `configs.py` and `multi_config_experiment.ipynb`
- Created `experiment/presentation design/` folder for design and technical documentation

### 2. Design and technical documentation
- Created `slider_plot_guideline.md` — design doc describing the dashboard goal, slider behavior, plot layout, and open questions
- Created `technical_plan_slider.md` — technical doc describing the full implementation plan for `build_dashboard.ipynb` and `dashboard.html`
- Key design decisions documented:
  - `n` and `d` are fixed for now; only `k` slides, displayed as `k/n` ratio
  - Plotly.js will be bundled inside the HTML (fully offline, no CDN)
  - Checkboxes for each metric (toggleable)
  - Side-by-side current vs previous ratio plots

### 3. Bug fix in `plotting.py`
- `compute_metrics` was missing `angle_w_star` and `angle_w_tilde` fields
- Fixed: angles (in degrees) now computed and returned directly from `compute_metrics`
- `plot_from_data` updated to use precomputed angles instead of recomputing inline
- Bug documented in `bugs_and_errors_documentation/2026-03-25_compute_metrics_missing_angles.md`

### 4. pkl files now store run settings
- Added `track_population_loss` and `pop_samples_per_dim` to the config saved in each `.pkl`
- `model.py`: these are now stored as `self.` attributes inside `run_gd`
- `configs.py`: reads them from the model and saves them in the config dict

### 5. CLAUDE.md updated
- Added rule: do not change things unless explicitly asked
- Added rule: prefer running scripts over predicting output by hand; avoid loading large data into context unnecessarily

## What Was NOT Done
- `build_dashboard.ipynb` not started yet
- `dashboard.html` not built yet

## Next Steps
1. Build `build_dashboard.ipynb` (load pkls → compute metrics → group by k/n → serialize JSON → build HTML)
2. Build `dashboard.html` with bundled Plotly.js, slider, and checkboxes

— Claude Sonnet 4.6
