# Project State

> Keep this file up to date. Edit it manually or ask the assistant to update it at the end of each session.
> Session summaries in `session_summaries/` are the full archive; this file is the living snapshot.

---

## What This Project Is

Empirical/experimental study of the paper:
**"Benefits of Early Stopping in Gradient Descent for Overparameterized Logistic Regression"**
Wu, Bartlett, Telgarsky, Yu — ICML 2025 (arXiv:2502.13283v2)

The main question: does the theoretical early stopping time (Theorem 3.1 sandwich condition) coincide with the argmins of other natural quantities (angle to w*, norm difference, angle to w̃)?

---

## Codebase Structure

```
experiment/
  model.py                  # Core GD model class
  configs.py                # Config helpers, run_and_save, resume_from_pkl
  plotting.py               # load_run, compute_metrics, plot_from_data
  multi_config_experiment.ipynb   # Run experiments + plot
  gen_dashboard_2.py        # Generates dashboard_2/dashboard.html
  gd_trajectories/          # Saved .pkl run files
  large_scale_experiments/  # Plans for batch runs
  dashboard_plans.md        # Dashboard specs

dashboard_1/dashboard.html  # Online dashboard (CDN Plotly, ~347KB)
dashboard_2/dashboard.html  # Single-Run Explorer (generated by gen_dashboard_2.py)
offline_dashboard_1/        # Offline version of dashboard_1 — deprecated, ~4.8MB
index.html                  # GitHub Pages entry point

references/
  main_paper.md             # Main paper transcribed to markdown (Sections 1–7) ← read this instead of the PDF
  whole_paper_appendix_included.md  # Full paper including appendix proofs ← use when proof details needed
  early_stopping_logistic_regression.pdf  # Original PDF (expensive to read — prefer .md versions above)

conjecture 1/               # Old folder (pre-refactor notebooks, kept for reference)
reviewing_the_code/
  sonnet_review/            # Prior Sonnet code reviews
```

---

## Dashboards

| Name | Folder | Description | Status |
|------|--------|-------------|--------|
| Dashboard 1 | `dashboard_1/` | Basic online viewer, CDN Plotly | Live on GitHub Pages |
| offline_dashboard_1 | `offline_dashboard_1/` | Offline version with inlined Plotly (~4.8MB) | Deprecated, still in repo |
| Dashboard 2 — Single-Run Explorer | `dashboard_2/` | Per-metric show/hide, normalize toggle, argmin toggle, seed+k/n sliders | Built, actively developed |
| Dashboard 3 — Multi-Run Explorer | `dashboard_3/` (planned) | Averaged view with confidence intervals, k/n slider | Planned, not yet built |

GitHub Pages: `https://alimorty.github.io/early_stopping/`

---

## Key Design Decisions (Locked)

- **Save raw trajectories, compute metrics at plot time** — flexible for adding new metrics later
- **`.pkl` files are self-contained** — include config, w_init, w_history, loss_history, pop_loss_history, w_tilde, stopping_times, timestamp, X, y
- **Early stopping is two-sided** — sandwich condition from Theorem 3.1: L̂(w_{t-1}) ≤ L̂(w*_{0:k}) ≤ L̂(w_{t-2})
- **Population loss samples scale with d** — `int(pop_samples_per_dim * d)` MC samples
- **Eigenvalues**: power-law `i^{-2}` by default
- **w\*** = first k components = 1, rest = 0

---

## Current Status (as of 2026-05-04)

### Done
- [x] Core GD model (`model.py`) with early stopping detection, population loss tracking
- [x] Config/save/load helpers (`configs.py`)
- [x] Plotting pipeline (`plotting.py`)
- [x] Dashboard 1 (online, GitHub Pages)
- [x] Dashboard 2 — Single-Run Explorer with per-metric normalize/argmin toggles, disabled row state
- [x] Paper transcribed to markdown (`references/main_paper.md`)
- [x] Code review folder organized (`reviewing_the_code/sonnet_review/`)
- [x] Large-scale experiment plan written (`experiment/large_scale_experiments/plans.md`)

### In Progress / Next Steps
- [ ] **Code verification (interactive)**: Ali is walking through the code step-by-step before scaling up. Checklist in `requests/verification_checklist.md`. Not started yet.
- [ ] **Dashboard 3** (Multi-Run Explorer): M trajectories averaged with confidence bands. Plan in `requests/dashboard_3_plan.md`. Blocked on verification.
- [ ] **Batch runner script**: loop over seeds and k values, structured output dirs, error handling
- [ ] **Fix dashboard_1 stopping line**: may have log-axis alignment bug (trace-based `y: [-10, 200]`)
- [ ] **Decide**: remove `offline_dashboard_1` from repo?

---

## Open Questions

- Does the theoretical stopping time (Theorem 3.1) coincide with the argmins of angle(w_t, w*), norm_diff, angle(w_t, w̃)?
- How robust is this across different seeds, k values, covariance structures?
- Does E[w̃] converge to w*? (preliminary positive signal at M=50, needs M=200+)

---

## Known Bugs

- `dashboard_1` stopping line may be misaligned on log x-axis (not yet investigated)
- `offline_dashboard_1` had iPad rendering issue (no longer prioritized)
